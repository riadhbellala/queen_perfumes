// Shared business constants for the admin dashboard — kept in one place so a
// value like the low-stock threshold can't drift between the dashboard's
// stat query and the Perfumes table's own badge/filter logic.
export const LOW_STOCK_THRESHOLD = 5;
